Task 1: Replace the Map, Editor, Link, Copy name and pin icons across the site with the ones in /Users/samjordan/github/samsdayzobjectfinder/Task Docs/SVG. First you need to change their colour to EEB224 - create a hover effect where there icons to white and the background goes EEB224. Our aim it unify the icon set access the the site and reduce the use of emojis. 

Task 2: We currently do linked-p3ds and config-variants by guessing - instead we should use a proper source. Review '/Users/samjordan/github/samsdayzobjectfinder/Task docs/models.csv’. Using the resolved_model column, match classnames in database to configs in database. Also match config variants to each other. These should still be added in build.py to the final export - ensure that is updated.

Task 3: I have added a folder at /Users/samjordan/github/samsdayzobjectfinder/Task Docs/dayz locations - it includes new builds to be added to Editor Builds. It includes an agents.md of waht you need to do.

Run through all these tasks - be prepared to revert 1 or more of them if not correct. Provide details once complete.